@model IEnumerable<SIPADE.Models.SipTblTipoActividad>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SipTblActNombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SipTblActCostoact)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SipTblActNombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SipTblActCostoact)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SipTblActId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SipTblActId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SipTblActId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
