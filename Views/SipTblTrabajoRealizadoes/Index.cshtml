@model IEnumerable<SIPADE.Models.SipTblTrabajoRealizado>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SipTblTreCantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SipTblTreTotal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SipTblAct)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SipTblEmp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SipTblTreCantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SipTblTreTotal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SipTblAct.SipTblActId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SipTblEmp.SipTblEmpId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SipTblTreId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SipTblTreId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SipTblTreId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
